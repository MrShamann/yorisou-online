import { NextResponse } from "next/server";
import { getViewerContext } from "@/lib/server/yorisouAuth";
import { actionRecommendation, getRecommendationSet, reportRecommendation } from "@/lib/server/recommendationGraph";
async function owner(){const viewer=await getViewerContext();return viewer.account?.id||viewer.legacyAccount?.id||null;}
export async function GET(_request:Request,{params}:{params:Promise<{id:string}>}){const account=await owner();if(!account)return NextResponse.json({error:"authentication_required"},{status:401});try{return NextResponse.json(await getRecommendationSet(account,(await params).id));}catch{return NextResponse.json({error:"not_found"},{status:404});}}
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const account=await owner();if(!account)return NextResponse.json({error:"authentication_required"},{status:401});try{const body=await request.json() as Record<string,unknown>,id=(await params).id;if(body.action==="report")return NextResponse.json(await reportRecommendation(account,id,String(body.reason)));return NextResponse.json(await actionRecommendation(account,id,String(body.action),typeof body.idempotencyKey==="string"?body.idempotencyKey:crypto.randomUUID(),typeof body.note==="string"?body.note:undefined));}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"action_failed"},{status:422});}}
