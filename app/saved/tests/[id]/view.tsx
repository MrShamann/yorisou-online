"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import PrivateStatePanel from "@/app/components/PrivateStatePanel";
type Saved = { id: string; test_id: string; test_version: string; result_title: string; public_summary: string; score_summary: { topDimensions: { label: string }[] } };
export default function SavedTestResultView({ id }: { id: string }) { const [saved, setSaved] = useState<Saved | null>(null); const [state, setState] = useState<"loading" | "missing" | "error">("loading"); useEffect(() => { fetch(`/api/tests/results/${id}`).then(async (response) => { if (!response.ok) return setState("missing"); setSaved((await response.json() as { saved: Saved }).saved); }).catch(() => setState("error")); }, [id]); const remove = async () => { if (!confirm("この保存結果を削除しますか？")) return; const response = await fetch(`/api/tests/results/${id}`, { method: "DELETE" }); if (response.ok) setState("missing"); else setState("error"); }; return <main className="min-h-screen bg-[var(--bg)] text-[var(--text)]"><div className="container py-10"><div className="mx-auto max-w-[44rem] rounded-[1.35rem] bg-white/90 p-6">{saved ? <><p className="service-kicker">保存した結果</p><h1 className="display-serif mt-2 text-3xl">{saved.result_title}</h1><p className="mt-4 leading-8 text-[#5F5750]">{saved.public_summary}</p><p className="mt-4 text-sm text-[#49615B]">見えた傾向: {saved.score_summary.topDimensions.map((item) => item.label).join("、")}</p><div className="mt-6 flex flex-wrap gap-3"><Link href={`/tests/${saved.test_id.toLowerCase()}`} className="btn inline-flex rounded-full px-5 py-3">もう一度試す</Link><Link href={`/experiences?source=${id}`} className="rounded-full border px-5 py-3 text-sm">この結果から体験カードを作る</Link><button type="button" onClick={remove} className="rounded-full border border-[rgba(155,58,52,0.25)] px-5 py-3 text-sm text-[#9b3a34]">この結果を削除</button></div><PrivateStatePanel resultId={id} /></> : <><p>{state === "loading" ? "保存した結果を開いています。" : state === "error" ? "結果を操作できませんでした。" : "この結果は開けません。"}</p><Link href="/tests" className="btn mt-5 inline-flex rounded-full px-5 py-3">テスト一覧へ</Link></>}</div></div></main>; }
