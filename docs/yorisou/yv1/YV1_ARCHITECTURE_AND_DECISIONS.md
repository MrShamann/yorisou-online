# YV-1 — Architecture Decision Record & Repository Dependency Map

**Package:** YV-1 yorisou-values「いま大事にしたいことチェック」scored vertical slice · **Base:** `main @ e6dd59d` · **Founder decision:** `YORISOU_YV_1_CANONICAL_CONTENT_APPROVED_FOR_GATED_IMPLEMENTATION` · **Status:** gated, non-public implementation candidate. Registry `implementation: in_progress` (activation `gated`). This is NOT merge, activation, hosted migration, or public availability.

## 1. Canonical source & loading (§4/§8)
Single source of truth: `docs/yorisou/mtf2a/yorisou-values.v1.json` (BYTE-FROZEN; bank hash `919f17251a280bb34258f6042db46bb9fd543763b33e041de64c36b305eaa9a6`). Strategy: **deterministic generated artifact** via `scripts/generate-yorisou-values-runtime.mjs` — re-verifies the pinned bank hash (sha256 over the compact serialization of `questionBank.items`) BEFORE generation, distills the runtime subset (no Japanese copy rewritten), emits `lib/yorisou/methods/yorisou-values/definition.generated.ts` marked GENERATED with the source hash. Drift fails the contract test (`--check` byte comparison + independent hash reproduction). Regenerate: `npm run generate:yorisou-values`.

## 2. Scored runtime boundary (§7) — NOT a generalized engine
`lib/yorisou/method-runtime/scored.ts` — channel-neutral, `scored` execution model ONLY (explicit rejection of others; `bank_hash_mismatch` on canonical drift). Independent from the DCI `recorded_state` runtime. Pairwise win-rate: `dimensionScore = wins / canonical appearances` (13/14 structure); reads the dimension attached to the selected canonical choice (never the A/B side); answer order and A/B side order never change the outcome. Coverage: 48/48 required → 0..47 = `insufficient_coverage` (no primary/secondary/Mixed, no imputation, exact remaining). Primary = highest win-rate; secondary = 2nd-highest (secondary SIGNAL, never a second result). Mixed rule: `top1 - top2 < 0.05` (no head-to-head conjunct). Tie-break: declaration order `[anshin, pace, tsunagari, seicho, yakuwari, totonoi, jikkan]`, secondary-ordering only. **Internal win rates live only inside the runtime envelope's `internal` field and are stripped by `assembleYorisouValuesResult` before any public/private payload — never in UI, share, URLs, HTML attrs, logs, analytics, API responses or export.** No universal score; no cross-method numeric comparison.

## 3. Result assembly (§5)
`lib/yorisou/methods/yorisou-values/scoring.ts` maps the runtime envelope to canonical public + private content (7 dimension-led results + `VAL_R_MIXED`). Mixed renders the close set (`同じくらい近かった軸`, declaration order); clear results render the secondary signal (`もうひとつ近かった軸`). `resultVariant: ArchetypeResult`; confirmation required. No internal numerics in the returned object.

## 4. Registry reconciliation (§16)
The pre-existing contract-level `yorisou-values` entry (`values-v0.1` "価値観リフレクション" placeholder, `psychology_preference`) was RECONCILED IN PLACE to the concrete YV-1 method (same `methodId`, no duplicate identity): `values-v1.0`「いま大事にしたいことチェック」, `yorisou_original_assessment`, scored. Truthful maturity on this branch: `implementation: in_progress` (candidate — NOT complete, so derived state stays exactly `gated`), `content: authored` (canonical frozen), `tests: passing` (contract suite), rights YORISOU-owned + cleared (not public-activated), `routeEvidence: none`, `deploymentStatus: unverified`, `founderActivation: closed`, `devFlagged: true`. `daily-check-in` unchanged (DCI-1-MERGE `complete`). No other method modified.

## 5. Persistence (§13/§14)
Merged-main storage rejected (result-shaped, CHECK-constrained `test_id`, single-active-row, no 48-answer provenance, no version history, no content-erasing deletion). Dedicated additive migration `supabase/migrations/202607210001_yv1_values_assessments.sql` (mirrors accepted DCI-1.2 pattern): assessment record (retakes = distinct rows) + append-only versions + content-free tombstone events. Access: **DIRECT_USER_DENY + SERVER_REPOSITORY_OWNER_SCOPE + RPC_ONLY_DATABASE_MUTATION** — RLS enabled, no user policies, service_role SELECT-only, all mutation via SECURITY DEFINER RPCs (create/correct/delete/purge, fixed search_path). Retake = distinct record; answer correction = new version (recomputed; prior versions preserved); user confirmation (confirmed/not_quite/skipped) is a SEPARATE field that never rewrites the computed result. Deletion = governed content erasure (version rows + record row + prior created/corrected events removed → exactly ONE content-free tombstone with 12-month `retention_expires_at`). Executable bounded purge `yorisou_values_tombstone_purge_expired` (service-role only; deleted+expired only; NO schedule). **No hosted apply; disposable-DB verification only; rollback executed (LOCAL_DISPOSABLE_SCHEMA_ROLLBACK).** The 12-month expiry is a GATED IMPLEMENTATION CANDIDATE — not an operating retention system; scheduling/hosted operationalization are separate Founder decisions.

## 6. Route & feature gating (§9)
`/tests/yorisou-values` + `/api/tests/yorisou-values/**`, all gated server-side by `lib/yorisou/methods/yorisou-values/access.ts` (pure over `deploymentContext`): local/test open; Vercel Preview requires the EXACT new flag `yorisou_values_preview` (no env var set by this package); **production + unknown → 404 (fail-closed)**. No nav card, no `/tests` catalog link, no sitemap, `robots: noindex`, no LINE entry. Client-only hiding is never the gate.

## 7. Resume provenance (§12)
`app/tests/yorisou-values/pendingProgress.ts` — device-local sessionStorage progress carrying method/bank/scoring/schema versions + bank hash + 24h expiry; `checkPendingValuesCompatibility` gates resume BEFORE any UI application (stale version/hash/expiry/malformed → honest Japanese notice, no coercion, fresh start). Server: create requires exact canonical provenance + hash else 422 `values_contract_version_mismatch`, no record created.

## 8. Recommendations (§15)
`hints.ts` — canonical governed tags only, private + consent-gated (default `no_recommendation`; user-initiated reveal); each hint carries the canonical fitReason + private-only boundary; no commercial/affiliate/supplier/payment/ranking. No generalized recommendation engine.

## 9. Anti-screening boundary (§11)
Interpretation limits + non-scientific boundary shown prominently in the intro and review; canonical `usageBoundaryJa` displayed at completion. A bounded anti-screening clause added to `app/terms/page.tsx` (§4-2「チェック結果の利用範囲」) without rewriting unrelated legal content.
