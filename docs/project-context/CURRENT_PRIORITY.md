# YORISOU — Current Priority

*(Package 20, 2026-07-15 · canonical; live authority = `vaults/projects/yorisou-online/00_Control/CURRENT_PRIORITY.md` in AI-Workspace)*

## Primary focus

**PAUSED — no active product package.** Production is live and verified at `334a8057`; the next product step is a founder decision.

## Queued items (secondary, none started)

1. **YRR-1** — founder-deferred a11y item (transient aria-hidden-focus on `/result` reveal window); revisit on its recorded triggers. Non-blocking; not fixed.
2. **990-question bank activation decision** — content-complete (founder-reported), NOT activated; requires taxonomy/scoring impact analysis under a founder package.
3. `governance/v0.4.0-activation` branch disposition.
4. P3 dev-tooling refresh (dev-only OSV HIGHs).

## Decisions not to reopen casually

Result taxonomy and scoring algorithm (contract-tested product identity); the non-medical boundary; consent-first LINE posture (no automatic sending); Japanese-language public product voice; the 120-question runtime as active set (until a founder activation package says otherwise).
